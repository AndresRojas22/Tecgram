<header class="bg-white p-5 shadow">
    <div class="container flex mx-auto justify-between items-center">
        <a href="{{route('index')}}"><h1 class="text-3xl font-black">TecGram</h1></a>
        <nav class="flex items-center gap-5">
            <a class="font-bold text-md uppercase text-gray-700" href="{{route('login')}}">Login</a>
            <a class="font-bold text-md uppercase text-gray-700" href="{{route('account.index')}}">Create account</a>
        </nav>
    </div>
</header>